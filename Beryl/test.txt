let
    const m ~ 7;
    const bar ~ 42;
    var n : Integer;
    func foo() : Integer ~
        bar;
    func f(a : Integer) : Integer ~
        a * foo()
in
    begin
        let
            const foo ~ 32
        in
            begin
                n := f(2) * m * m + foo;
                putint(n)
            end
    end

